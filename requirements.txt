# Networking & scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
urllib3>=2.0

# Config & CLI
pyyaml>=6.0
click>=8.1

# Data handling
pandas>=2.0

# Rate limiting / politeness
tldextract>=5.1

# PII detection (lightweight regex-based, no heavy NLP deps required)
# Optional, heavier NLP-based PII detection (nomi propri, luoghi):
# abilita con output.use_ner: true in config/sources.yaml. Richiede:
#   pip install spacy
#   python -m spacy download it_core_news_sm
# spacy>=3.7

# Optional: language detection for output.use_language_filter in
# config/sources.yaml. Senza questa dipendenza il campo "language" resta
# null e nessun filtro viene applicato (degrado morbido, nessun errore).
#   pip install langdetect
# langdetect>=1.0.9
